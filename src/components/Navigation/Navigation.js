import React from 'react';

const Navigation = () => {
    return (
        <nav style={{display: 'flex', justifyContent: 'flex-end'}}>
            <button className='btn pa2 dim w-10 f5 link pointer ma2'>Sign Out</button>
        </nav>
    )
}

export default Navigation;