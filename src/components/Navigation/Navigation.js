import React from 'react';

const Navigation = () => {
    return (
        <nav style={{display: 'flex', justifyContent: 'flex-end'}}>
            <button className='btn pa2 ph4 dim f5 link pointer ma2'>Sign Out</button>
        </nav>
    )
}

export default Navigation;